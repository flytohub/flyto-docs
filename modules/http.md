# HTTP

Standalone HTTP request module for simple GET requests. For more advanced HTTP operations, see [Atomic](/modules/atomic) (http.request, http.session, http.paginate) or [API Tools](/modules/api-tools).

## Modules

| Module | Description |
|--------|-------------|
| `http.get` | Send HTTP GET request |
