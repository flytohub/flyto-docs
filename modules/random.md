# Random

Modules for generating random values -- numbers, UUIDs, selections, and shuffled arrays.

## Modules

| Module | Description |
|--------|-------------|
| `random.choice` | Pick random element from array |
| `random.number` | Generate random number in range |
| `random.shuffle` | Shuffle array randomly |
| `random.uuid` | Generate UUID v4 |
