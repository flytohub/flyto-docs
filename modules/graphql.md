# GraphQL

Modules for executing GraphQL queries and mutations against any GraphQL endpoint.

## Modules

| Module | Description |
|--------|-------------|
| `graphql.mutation` | Execute a GraphQL mutation |
| `graphql.query` | Execute a GraphQL query |
