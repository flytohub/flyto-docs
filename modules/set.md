# Set

Set theory operations on arrays -- union, intersection, difference, and deduplication.

## Modules

| Module | Description |
|--------|-------------|
| `set.difference` | Elements in first set not in second |
| `set.intersection` | Elements common to all sets |
| `set.union` | Combine all unique elements |
| `set.unique` | Remove duplicates from set |
