# Environment

Modules for managing environment variables. Load from `.env` files, get, and set runtime environment values.

## Modules

| Module | Description |
|--------|-------------|
| `env.get` | Get environment variable |
| `env.load_dotenv` | Load variables from .env file |
| `env.set` | Set environment variable |
