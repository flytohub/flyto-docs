# Wrap-up workflow

1. Run `npm run docs:build`.
2. Update `STATE.md`, `tasks.md`, or a handoff.
3. Check `git status --short`.
4. Commit, push, fetch, and verify local `main` equals `origin/main` for release
   work.
