# Bugfix workflow

1. Reproduce the docs build or rendering issue.
2. Identify whether the issue is markdown, VitePress config, routing, or public
   crawler metadata.
3. Fix narrowly.
4. Run `npm run docs:build`.
