# Planning workflow

1. Read project memory files.
2. Map the docs change to product line and public URL.
3. Update navigation, sidebar, llms files, and structured metadata as needed.
4. Define `npm run docs:build` as the minimum verification command.
