# Refactor workflow

1. Preserve public URLs or add redirects outside this repo.
2. Keep llms and sitemap references aligned.
3. Avoid moving pages across product lines without updating navigation and
   product-line docs.
4. Run `npm run docs:build`.
