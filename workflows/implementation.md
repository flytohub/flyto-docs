# Implementation workflow

1. Keep headings answer-engine friendly.
2. Keep examples deterministic and safe for enterprise readers.
3. Update `public/llms.txt` for new high-level citation pages.
4. Run `npm run docs:build`.
5. Record unresolved docs gaps in `tasks.md` or a handoff.
